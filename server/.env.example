# Server Signer Private Key (DO NOT COMMIT ACTUAL KEY)
# Generate with: cd contracts && npx hardhat run scripts/generateSigner.ts
SERVER_SIGNER_PRIVATE_KEY=0x...

# AbyssalVault Contract Address on Base mainnet
# Set after deploying: cd contracts && npm run deploy:base
VAULT_ADDRESS=0x...

# WebSocket port (Railway sets PORT automatically)
PORT=8080
